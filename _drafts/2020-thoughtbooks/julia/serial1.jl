# caches.
#

rand(10,10)

typeof(4.0)
#=
staticarrays

@which 
@view - not views by default. huh
@inbounds
@boundscheck

static arrays

use . braodcasting foir loop fusion and bounds checking

function barriers




    using Flux
NN= Chain(Dense(), Dense, Sense)

loss() = 

Flux.train!( loss, )

=#

function h(x,y)
    out = x + y
    rand() < 0.5 ? out : Float64(out)
end

@code_warntype

typeof(2) <: Any
isbits


